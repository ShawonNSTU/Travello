<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/red</item>
        <item name="colorPrimaryDark">@color/darkRed</item>
        <item name="colorAccent">@color/color_Accent</item>
    </style>

    <style name="dialog_animation">
        <item name="android:windowEnterAnimation">@anim/dialog_pop_up_anim</item>
    </style>
    
    <style name="full_screen_window" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="RatingBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/next</item>
        <item name="colorControlActivated">@color/next</item>
    </style>

    <style name="fade_in_animation">
        <item name="android:windowEnterAnimation">@anim/fade_in_anim</item>
    </style>
    
    <style name="SwitchCompatColor" parent="Theme.AppCompat.Light">
        <item name="colorControlActivated">@color/next</item>
    </style>

</resources>
